# StockTradebyZ Web

React/Vite/TypeScript frontend for the product-level refactor.

## Local Commands

```bash
npm install
npm run dev
npm run build
npm run lint
```

The Vite dev server proxies `/api` to `http://127.0.0.1:8000`.
